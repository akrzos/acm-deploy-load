---
# talm-deploy default vars

talm_default_channel: stable

# Used with TALM from a repo only
talm_precache_image_to_replace: quay.io/openshift-kni/cluster-group-upgrades-operator-precache:4.19.0
talm_recovery_image_to_replace: quay.io/openshift-kni/cluster-group-upgrades-operator-recovery:4.19.0

# Used with TALM from a repo only
talm_thread_count: 0
